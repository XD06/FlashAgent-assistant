# Changelog

## Unreleased

### Changed

- 项目对外名称、包名、安装包和发布产物统一为 `FlashAgent-assistant`；首次启动时会将旧 `selection-assistant-lite` 用户数据目录迁移到新目录，且绝不覆盖已有的新目录。
- README、贡献指南、E2E 指引和文档索引已更新；历史方案文档已明确标记为归档材料，避免与当前实现混淆。

### Added

- 跨轮历史原生工具消息回放：assistant 消息内嵌完整工具轨迹（调用参数 + 结果），发送历史时按协议展开为原生 `tool_calls`/`tool` 消息（OpenAI-compatible）或 `tool_use`/`tool_result` 块（Anthropic），对齐两家官方多轮工具调用标准；文字台账仅保留在压缩摘要内并继续防伪剥离。从根本上消除模型模仿文本台账伪造工具结果的叙述型幻觉。
- CJK 感知 token 估算：中/日/韩字符按 ~0.7 token/字估算（旧 `chars/4` 规则对中文低估近 3 倍），压缩触发、发送截断、进度环三处统一换算；按消息对象 WeakMap 缓存估算结果，长对话不重复全量扫描。
- Agent 上下文管理升级为 token 驱动：流式请求采集真实 usage（`prompt_tokens`），上下文占用进度环改为 token 口径优先显示。
- 轮间同步压缩（pre-send）：上下文超过 128k 注意力预算的 95% 时，发送下一条消息前先压缩旧历史再发正式请求，压缩结果立即服务当前轮；拿不到 usage 的 provider 自动回退字符闸门，不留死角。
- 压缩产物升级为五节「复工文档」（原始任务 / 关键结论与事实 / 涉及文件与当前状态 / 已尝试且失败的路径 / 下一步），路径、命令、数值逐字保留，压缩后模型可无缝接上任务。
- LLM 调用自动重试与退避：429/5xx/网络瞬断按 1s→2s→4s 重试 3 次（`Retry-After` 优先），长任务不再因单次抖动中断；重试过程有状态行提示。
- 工具参数校验：非法 JSON / 缺失字段 / 类型错误直接回传可行动错误文本让模型自纠，不再弹审批卡片、不再以空参数静默执行。
- `edit_file` 新增行尾空白容错匹配与行号级失败诊断；`read_file` 输出标注文件换行符类型（CRLF/LF）。
- 新增真实 E2E 回归工具 `pnpm e2e:compaction`（`sandbox-e2e/`）：用本机真实 API 配置走生产代码链，验证 usage 采集→压缩触发→复工文档→压缩后连续性全流程。
- 话题自动命名：首轮对话完成后由模型生成简短标题（复用压缩模型偏好，失败静默保留截断式默认标题）；用户手动重命名始终优先，自动保存不会覆盖。
- 上下文窗口可配置：设置→功能模型新增「上下文窗口（tokens）」，按所用模型填写（默认 128000），压缩触发线为其 95%，进度环与判定实时联动。
- 命令/工具输出终端化渲染：解析程序原生 ANSI 颜色（pytest/vite/eslint 等自带色，含 16/256/真彩色）；无 ANSI 输出仅对行首状态标记（OK/PASS/FAIL/ERROR/WARN/✓✗/退出码）克制上色，不做字段级解析；命令输出改为中性深底终端配色，不再整体强染绿色。
- 工具输出可读性优化：编辑/写入改为标准 unified diff（旧行 `-` 红、新行 `+` 绿），一眼看清「谁替换了谁」；读取文件的行号 gutter 染为次要灰、代码正文保持中性；读取/写入/MCP 结果块去掉彩色背景 wash，统一为单一中性底 + 类别左边框细线；命令终端底色由纯黑 `#1e1e1e` 调整为柔和的 One Dark `#282c34`。
- Windows 无 Git Bash 时自动回退 PowerShell，`run_command` 工具描述根据实际 shell 动态生成（提示模型改用 PowerShell 语法）；子进程注入 `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8` 消除 Python 管道输出中文乱码。
- 历史对话列表改为 Codex 风格：列表行只留标题与时间/轮数，重命名 / 导出 Markdown / 删除收进右键菜单（Esc 或点击空白关闭，删除项红色警示）；侧边栏宽度随之收窄。
- 任务中追加的消息改为就地显示：气泡出现在工具循环实际收到它的位置（思考/工具/正文块之间），不再跳到整个回复最上方；回放、压缩、导出均同步保留追加内容。
- 修复扩展面板小图标按钮（MCP 删除、Skills 取消链接、刷新/关闭）被全局 button 样式挤压导致 SVG 不可见的问题；破坏性图标按钮 hover 变红以示区分。
- 思考过程折叠/展开使用同一标签（箭头已表达状态），去掉「（已折叠）」后缀噪音。
- 命令执行环境可选（Windows）：扩展面板→权限页新增「命令执行环境」下拉项（自动 / Git Bash / PowerShell 7 (pwsh) / Windows PowerShell 5 / CMD）；选定后 `run_command` 工具描述与系统提示词会明确告知模型当前终端及语法要求（如 PowerShell 用 `;` 串联、Select-String 代替 grep），减少 bash / PowerShell 语法混用错误；所选终端未安装时自动回退（Git Bash / pwsh → Windows PowerShell 5），描述永远如实反映实际执行器。
- 滚动条全局隐藏：主界面与对话窗口不再绘制任何滑条（滚轮/触控/键盘滚动不受影响），去掉原 2px 细滑条的视觉噪音。
- 对话输入框改为悬浮两行式卡片（Grok/ChatGPT 风格）：上行整行输入，下行左侧功能按钮（联网/Agent/工作目录/新对话/上下文环）、右侧圆形实心发送键；停止键改为红色圆形软底样式。
- 标题栏与图标质感优化：全局图标描边由 2.0 细化为 1.75 更精致；重绘星光（曲线四芒星）、扩展（四宫格）、历史（侧栏式）、图钉（竖直推针）图标；最小化由文本「-」换为 SVG；标题栏按钮改圆形 hover 底呼应输入框风格，关闭键 hover 呈危险红色。
- 主界面（设置窗口）改为横向布局：窗口加宽至 720×600，左侧竖排图标导航（窄窗自动折叠为纯图标栏），右侧内容区独立滚动；顶栏新增应用品牌标识（图标+名称），语言切换与开关状态归组右侧；API 页表单双列排布（长字段跨整行），设置行加大内边距与控件宽度，分组间距拉开层次。
- 主界面（设置窗口）四页风格对齐对话界面：顶部导航改胶囊式分段并为每页配图标（API=链接、动作=指针、划词=扫描、窗口=笔记本）；卡片圆角加大至 14px 并加微阴影；「+ 添加」按钮与动作排序 ↑↓ 由文本字符换为 SVG 图标；小按钮统一圆形，删除键 hover 呈危险红底；修复 add-row-button hover 引用不存在的 --primary 变量。

### Fixed

- 彻底禁用 selection-hook 的剪贴板回退（模拟 Ctrl+Insert/Ctrl+C 抓取选区文本）：该回退会在终端里把 Ctrl+C 变成 SIGINT 杀掉正在运行的 CLI，也会在桌面长按/连点等误判场景触发模拟复制（剪贴板管理器误响、剪贴板历史被污染）。现在划词取词只依赖无侵入的 UIA/IAccessible 接口，极少数不支持无障碍接口的老应用将静默跳过而非模拟按键。

### Tests

- 测试用例扩充至 282 个；新增原生工具回放展开（OpenAI/Anthropic 角色序列、严格交替合并、坏参数降级）、CJK token 估算、trace 计量与截断原子性、压缩触发判定、重试退避、参数校验、edit_file 诊断、ANSI 终端渲染、shell 选择与工具描述重写等回归用例。

## 0.7.0 - 2026-07-27

### Added

- 对话窗口全面升级：多话题历史记录（本地持久化、可切换 / 删除 / 导出）、话题自动命名。
- 长对话自动压缩：超出阈值时用 LLM 将较早消息压缩为结构化前情摘要，可指定专用压缩模型，压缩过程界面有胶囊提示。
- Agent 模式：模型可调用内置工具（读写文件、编辑文件、执行命令、列目录等），工具调用逐条确认，支持会话级「始终允许」。
- 工具回退：文件改动自动快照并落盘（跨重启有效），可一键回退单次工具修改。
- 命令三层风险分级：毁灭性命令（`rm -rf /`、`format c:` 等）无条件拦截；危险命令（`rm`、`git reset --hard`、`taskkill` 等）任何模式下都强制二次确认；普通命令走常规审批。
- 权限模式：扩展面板新增「权限」页，可开启完全访问模式（免逐条确认），但危险命令确认与毁灭性拦截不受影响。
- 扩展系统：记忆（写入需用户确认）、Skills 技能、MCP 服务器接入（MCP 工具调用恒需确认）。
- 功能模型设置：识图模型、压缩模型可独立指定；每个划词动作也可覆盖模型名。
- 对话导出：历史记录中每条对话可一键导出为 Markdown，写入下载目录并在资源管理器中定位。
- `edit_file` 工具支持 `replace_all` 批量替换。

### Fixed

- 修复截图 AI 识图未使用所配置识图模型的问题（设置异步加载导致的陈旧闭包）。
- 修复导出对话时原生保存对话框与划词底层钩子消息循环死锁、导致应用无响应退出的问题。
- 修复结果窗口与识图窗口流式渲染逐 token 重渲染的性能问题，改为 50ms 合批（长回复不再卡顿）。

### Tests

- 测试用例扩充至 164 个，覆盖 Agent 工具、命令风险分级、动作规范化、对话压缩等。

## 0.6.2 - 2026-07-05

### Fixed

- 修复划词悬浮工具栏在显示时可能抢占原应用文本焦点的问题，降低与鼠标手势、剪贴板增强等工具的冲突概率。

## 0.6.1 - 2026-06-16

### Fixed

- 修复设置页服务名称输入中文时被异步设置刷新打断，导致 IME 组合输入异常的问题。
- 修复服务名称清空后会自动恢复为 `Template 1`，导致默认名称首字母无法删除的问题。
- 修复 Windows 便携版中模型列表获取、模型测活和翻译请求可能因 Node `fetch` 未走 Electron 网络栈而报 `fetch failed` 的问题。
- 兼容 OpenAI-compatible 配置中粘贴完整 `/chat/completions` 或 `/models` endpoint 的常见用法。

### Tests

- 增加服务名称清空、OpenAI-compatible URL 规范化、供应商请求 fetcher 注入和 Web Search 预判请求的回归测试。
