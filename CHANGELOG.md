# Changelog

## 0.7.0 - 2026-07-27

### Added

- 对话窗口全面升级：多话题历史记录（本地持久化、可切换 / 删除 / 导出）、话题自动命名。
- 长对话自动压缩：超出阈值时用 LLM 将较早消息压缩为结构化前情摘要，可指定专用压缩模型，压缩过程界面有胶囊提示。
- Agent 模式：模型可调用内置工具（读写文件、编辑文件、执行命令、列目录等），工具调用逐条确认，支持会话级「始终允许」。
- 工具回退：文件改动自动快照并落盘（跨重启有效），可一键回退单次工具修改。
- 命令三层风险分级：毁灭性命令（`rm -rf /`、`format c:` 等）无条件拦截；危险命令（`rm`、`git reset --hard`、`taskkill` 等）任何模式下都强制二次确认；普通命令走常规审批。
- 权限模式：扩展面板新增「权限」页，可开启完全访问模式（免逐条确认），但危险命令确认与毁灭性拦截不受影响。
- 扩展系统：记忆（写入需用户确认）、Skills 技能、MCP 服务器接入（MCP 工具调用恒需确认）。
- 功能模型设置：识图模型、压缩模型可独立指定；每个划词动作也可覆盖模型名。
- 对话导出：历史记录中每条对话可一键导出为 Markdown，写入下载目录并在资源管理器中定位。
- `edit_file` 工具支持 `replace_all` 批量替换。

### Fixed

- 修复截图 AI 识图未使用所配置识图模型的问题（设置异步加载导致的陈旧闭包）。
- 修复导出对话时原生保存对话框与划词底层钩子消息循环死锁、导致应用无响应退出的问题。
- 修复结果窗口与识图窗口流式渲染逐 token 重渲染的性能问题，改为 50ms 合批（长回复不再卡顿）。

### Tests

- 测试用例扩充至 164 个，覆盖 Agent 工具、命令风险分级、动作规范化、对话压缩等。

## 0.6.2 - 2026-07-05

### Fixed

- 修复划词悬浮工具栏在显示时可能抢占原应用文本焦点的问题，降低与鼠标手势、剪贴板增强等工具的冲突概率。

## 0.6.1 - 2026-06-16

### Fixed

- 修复设置页服务名称输入中文时被异步设置刷新打断，导致 IME 组合输入异常的问题。
- 修复服务名称清空后会自动恢复为 `Template 1`，导致默认名称首字母无法删除的问题。
- 修复 Windows 便携版中模型列表获取、模型测活和翻译请求可能因 Node `fetch` 未走 Electron 网络栈而报 `fetch failed` 的问题。
- 兼容 OpenAI-compatible 配置中粘贴完整 `/chat/completions` 或 `/models` endpoint 的常见用法。

### Tests

- 增加服务名称清空、OpenAI-compatible URL 规范化、供应商请求 fetcher 注入和 Web Search 预判请求的回归测试。
